var playlist = new Object({ artistname: 'songName'})

Object.assign({playlist: ''})

function updatePlaylist(playlist, songName, songTitle){
  
  return playlist
}

function removeFromPlaylist(playlist, artistname){
  
}